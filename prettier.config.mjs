const config = {
  singleQuote: true,
  jsxSingleQuote: true,
  semi: false,
  arrowParens: 'avoid',
  tailwindAttributes: ['myClassList'],
  plugins: ['prettier-plugin-tailwindcss'],
}
export default config
